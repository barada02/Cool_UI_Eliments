<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hanging Light Bulb</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Custom CSS for the hanging light bulb */
        body {
            font-family: 'Inter', sans-serif; /* Using Inter font */
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #1a202c; /* Dark background for contrast */
            margin: 0;
            overflow: hidden;
        }

        /* Container for the entire light bulb assembly */
        .light-bulb-container {
            position: relative;
            display: flex;
            flex-direction: column;
            align-items: center;
            transition: all 0.3s ease-in-out; /* Smooth transition for hover effects */
        }

        /* The wire/cord from which the bulb hangs */
        .light-bulb-cord {
            width: 4px; /* Thin cord */
            height: 150px; /* Length of the cord */
            background-color: #4a5568; /* Dark grey for the cord */
            border-radius: 2px; /* Slightly rounded cord */
            z-index: 1; /* Behind the bulb */
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3); /* Subtle shadow for depth */
        }

        /* The base/cap of the light bulb */
        .light-bulb-cap {
            width: 30px; /* Width of the cap */
            height: 40px; /* Height of the cap */
            background: linear-gradient(to bottom, #718096, #4a5568); /* Gradient for metallic look */
            border-radius: 5px 5px 15px 15px; /* Rounded top, more rounded bottom */
            position: relative;
            z-index: 2; /* Above the cord */
            box-shadow: 0 5px 10px rgba(0, 0, 0, 0.4); /* Shadow for depth */
        }

        /* The main glass part of the light bulb */
        .light-bulb-glass {
            width: 80px; /* Width of the bulb */
            height: 100px; /* Height of the bulb */
            background-color: rgba(255, 255, 255, 0.2); /* Slightly transparent for unlit state */
            border-radius: 50% 50% 50% 50% / 60% 60% 40% 40%; /* Elliptical shape for bulb */
            position: relative;
            z-index: 3; /* On top of the cap */
            border: 1px solid rgba(255, 255, 255, 0.3); /* Subtle border */
            box-shadow: inset 0 0 10px rgba(0, 0, 0, 0.2); /* Inner shadow for glass effect */
            transition: background-color 0.3s ease-in-out, box-shadow 0.3s ease-in-out; /* Smooth transitions */
        }

        /* The filament inside the bulb */
        .light-bulb-filament {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 2px; /* Thin filament */
            height: 30px; /* Length of the filament */
            background-color: #888; /* Dark grey for unlit filament */
            border-radius: 1px;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
            transition: background-color 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
        }

        /* The glow effect around the light bulb */
        .light-bulb-glow {
            position: absolute;
            width: 200px; /* Width of the glow */
            height: 200px; /* Height of the glow */
            background: radial-gradient(circle, rgba(255, 255, 200, 0.7) 0%, rgba(255, 255, 200, 0) 70%);
            border-radius: 50%; /* Circular glow */
            filter: blur(50px); /* Soften the glow */
            opacity: 0; /* Hidden by default */
            transition: opacity 0.4s ease-in-out; /* Smooth transition */
            z-index: 0; /* Behind all other elements */
            bottom: -50px; /* Position the glow below the bulb */
        }

        /* Hover effects for the light bulb */
        .light-bulb-container:hover .light-bulb-glass {
            background-color: rgba(255, 255, 255, 0.6); /* Brighter when lit */
            box-shadow: 0 0 20px 5px rgba(255, 255, 200, 0.8), /* Stronger glow from glass */
                        0 0 40px 10px rgba(255, 255, 200, 0.6);
        }

        .light-bulb-container:hover .light-bulb-filament {
            background-color: #ffeb3b; /* Yellow for lit filament */
            box-shadow: 0 0 15px 5px #ffeb3b, /* Glow from filament */
                        0 0 30px 10px rgba(255, 235, 59, 0.7);
        }

        .light-bulb-container:hover .light-bulb-glow {
            opacity: 1; /* Make the glow visible on hover */
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .light-bulb-cord {
                height: 100px;
            }
            .light-bulb-glass {
                width: 70px;
                height: 90px;
            }
            .light-bulb-glow {
                width: 180px;
                height: 180px;
                filter: blur(40px);
                bottom: -40px;
            }
        }

        @media (max-width: 480px) {
            .light-bulb-cord {
                height: 80px;
            }
            .light-bulb-glass {
                width: 60px;
                height: 80px;
            }
            .light-bulb-glow {
                width: 150px;
                height: 150px;
                filter: blur(30px);
                bottom: -30px;
            }
        }
    </style>
</head>
<body>
    <div class="light-bulb-container">
        <div class="light-bulb-cord"></div>
        <div class="light-bulb-cap"></div>
        <div class="light-bulb-glass">
            <div class="light-bulb-filament"></div>
        </div>
        <div class="light-bulb-glow"></div>
    </div>
</body>
</html>
